version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          command: curl hoges213123.com

  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - run: 
          name: Deployment
          command: echo "Success!" 
      - run:
          name: hoge
          command: curl hoges213123.com
          when: on_fail

workflows:
  build-permission-deploy:
    jobs:
      - build
      - deploy