version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          command: curl hoges213123.com

  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - run: 
          name: Deployment
          command: curl hoges213123.com
      - run:
          name: hoge
          command: echo hoge
          when: on_fail

workflows:
  build-permission-deploy:
    jobs:
      - build
      - deploy