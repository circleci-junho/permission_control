version: 2.1

jobs:
  build:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          command: echo /fail

  deploy:
    docker:
      - image: cimg/base:stable
    steps:
      - run: 
          name: Deployment
          command: echo "Success!" 
      - run:
          name: hoge
          command: echo "hoge"
          when: on_fail

workflows:
  build-permission-deploy:
    jobs:
      - build
      - deploy:
          context: permission
          requires:
            - build
